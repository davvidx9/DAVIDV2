## Cursor Cloud specific instructions

This repository currently contains only placeholder files (`README.md`, `TEST`, and `TEST V2`) plus an Angular/Node-style `.gitignore`; no runnable application source, package manifest, build config, test config, Docker/devcontainer config, or service definitions are present.

- The Cursor Cloud update script is intentionally `true` until dependency manifests are added.
- There are no required services to start for end-to-end testing at this time.
- Standard lint, test, build, and development server commands are not available until the repository gains the corresponding project files. Re-check `README.md`, package manifests, and any future docs before assuming commands.
